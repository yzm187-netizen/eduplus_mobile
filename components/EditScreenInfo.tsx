// DEPRECATED: Template component removed from usage. Safe to delete.
// Keeping a no-op export temporarily to avoid breaking unknown imports.
import React from 'react';
import { View } from 'react-native';

export default function EditScreenInfo() {
  return <View />;
}
